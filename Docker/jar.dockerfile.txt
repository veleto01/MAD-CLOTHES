# Selecciona la imagen base
FROM openjdk:17-jdk-slim
# Define el directorio de trabajo para el comando
WORKDIR /Madclothes/
# Copia de la aplicación compilada
COPY /Ejecutablesjava/hola.jar .
# Indica el puerto que expone el contenedor
EXPOSE 8443
#paso de parametros
ARG nombreInstancia=Madclothes
ENV NOMBRE_INSTANCIA ${nombreInstancia}
# Comando que se ejecuta al hacer docker run
CMD java -jar hola.jar --spring.datasource.url=jdbc:mysql://db_MADCLOTHES/Madclothes --spring.datasource.username=david --MAIL_SERVICE_URL=http://localhost:8444 --NOMBRE_INSTANCIA="${NOMBRE_INSTANCIA}"